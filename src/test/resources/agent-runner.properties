# ------------------------------------------------------------
# Выбор активного CLI (= имя его исполняемого файла).
# ------------------------------------------------------------
agent.cli=qwen

# ------------------------------------------------------------
# Qwen Code  (активен)
# ------------------------------------------------------------
agent.cli.qwen.args=--output-format,stream-json,--approval-mode,yolo
agent.cli.qwen.prefix.windows=cmd,/c
agent.cli.qwen.fallback.mac=${env.HOME}/.local/bin
agent.cli.qwen.fallback.linux=${env.HOME}/.local/bin

# ------------------------------------------------------------
# Codex (OpenAI)
# ------------------------------------------------------------
#agent.cli=codex

#agent.cli.codex.args=exec,--json,--dangerously-bypass-approvals-and-sandbox
#agent.cli.codex.prefix.windows=cmd,/c
#agent.cli.codex.fallback.mac=/opt/homebrew/bin/codex

# ------------------------------------------------------------
# Claude Code
# ------------------------------------------------------------
#agent.cli=claude

#agent.cli.claude.args=-p,--output-format,stream-json,--dangerously-skip-permissions
#agent.cli.claude.prefix.windows=cmd,/c
#agent.cli.claude.fallback.mac=${env.HOME}/.local/bin
